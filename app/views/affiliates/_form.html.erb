<%= form_for(affiliate) do |f| %>
  <% if affiliate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(affiliate.errors.count, "error") %> prohibited this affiliate from being saved:</h2>

      <ul>
      <% affiliate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :logoLink %>
    <%= f.text_field :logoLink %>
  </div>

  <div class="field">
    <%= f.label :moneyConfirmed %>
    <%= f.text_field :moneyConfirmed %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
