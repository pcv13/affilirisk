<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @affiliate.name %>
</p>

<p>
  <strong>Logolink:</strong>
  <%= @affiliate.logoLink %>
</p>

<p>
  <strong>Moneyconfirmed:</strong>
    <%= @M %>
<p>
  <strong>offers:</strong>
      
<table class="container" cellpandding="0" cellspacing="0" border="0">
  <thead>
    <tr>
      <th><h1>Id</h1></th>
      <th><h1>Name</h1></th>
      <th><h1>Installs</h1></th>
      <th><h1>Revenue</h1></th>

      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @offers.each do |request| %>
      <tr>
        <td><%= request.id %></td>
        <td><%= request.name %></td>
        <td><%= request.installs %></td>
        <td><%= request.revenue %></td>
        <td><%= request.risk %></td>
        <td><%= request.finalStatus %></td>
        <td><%= request.comment1 %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</p>

<%= link_to 'Edit', edit_affiliate_path(@affiliate) %> |
<%= link_to 'Back', affiliates_path %>
